name: Branch exclude type Workflow

on:
  push:
    branches-ignore:
      - main
    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "Build Successfull"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "Testing Successfull"
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "Deployed Successfully"
